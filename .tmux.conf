set -g terminal-overrides 'xterm:colors=256'

# enable mouse
# set -g mode-mouse on
set -g mouse on
set -g mouse-utf8 on

# change prefix
unbind C-b
set -g prefix C-a

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
